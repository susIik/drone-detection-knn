services:
  backend:
    image: frontend
    build: ./server
    ports:
      - "5001:5001"

  #frontend:
  #  image: backend
  #  build: ./client
  #  ports:
  #    - "3000:3000"

  listener:
    image: listener
    build: ./scripts
    devices:
      - "/dev/snd:/dev/snd"
    ports:
      - "8080:8080"